<%= simple_form_for(@gig, :html => { :class => 'form-horizontal' }) do |f| %>
	<%=	f.input :title %>
	<%=	f.input :description %>
	<%=	f.input :start_time %>
	<%=	f.association :venue %>
	<%=	f.association :bands %>
	<%= f.button :submit %>
<% end %>
