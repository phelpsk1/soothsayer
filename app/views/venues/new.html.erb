<h1>New Venue</h1>
<%= form_for :venue, url: venues_path do |f| %>
  <% if @venue.errors.any? %>
	<div id="errorExplanation">
		<h2><%= pluralize(@venue.errors.count, "error") %> prohibited
			this post from being saved:</h2>
		<ul>
		<% @venue.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
		</ul>
	</div>
	<% end %>

  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>

  <p>
    <%= f.submit %>
  </p>
<% end %>

<%= link_to 'Back', venues_path %>
