<%= form_for @band do |f| %>
  <% if @band.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@band.errors.count, "error") %> prohibited
      this band from being saved:</h2>
    <ul>
    <% @band.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
  <p>
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </p>
 
  <p>
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </p>
  
<p>
    <%= f.label :genre %><br>
    <%= f.text_area :genre %>
  </p>
 
	<p>
		<%= f.file_field :photo %>

  <p>
    <%= f.submit %>
  </p>
<% end %>
